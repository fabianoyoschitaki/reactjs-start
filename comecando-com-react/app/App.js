//import lib react
var React = require('react');
var ReactDOM = require('react-dom');

// first parameter is a JSX or a React component and the ID where it will be renderized
ReactDOM.render(<h1>teste</h1>, document.getElementById("app"));